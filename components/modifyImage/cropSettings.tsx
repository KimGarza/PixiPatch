import { View, StyleSheet, Text } from 'react-native';

const CropSettings = () => {
return (
    <View style={styles.container}>
        <Text>CROP SETTINGS</Text>
    </View>
);
}

export default CropSettings;

const styles = StyleSheet.create({
    container: {
    }
})