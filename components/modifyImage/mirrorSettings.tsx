import { View, StyleSheet, Text } from 'react-native';

const MirrorSettings = () => {
return (
    <View style={styles.container}>
        <Text>MIRROR SETTINGS</Text>
    </View>
);
}

export default MirrorSettings;

const styles = StyleSheet.create({
    container: {
    }
})